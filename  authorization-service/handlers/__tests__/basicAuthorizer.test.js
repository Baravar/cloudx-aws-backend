import { basicAuthorizer } from '../basicAuthorizer.js';

describe('basicAuthorizer', () => {
  it('...', async () => {
    const eventMock = {};
    await expect(basicAuthorizer(eventMock)).resolves.toBe(undefined);
  });
});
